import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Calculadora-Densidad-Madera',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
